/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/desafio-target',
    output: "export",  // <=== habilita o export estático
    reactStrictMode: true,
};

export default nextConfig;
